# Power BI delivery contract

Sprint 6 will ship a `.pbit` template with pages for Executive Summary, Portfolio Health, Risk, Sector Allocation, Fund Comparison, Forecast, Stock Drillthrough, and AI Recommendations.

The template will consume the versioned API contract rather than a developer's local database. Published release assets are exposed through `GET /powerbi`.
